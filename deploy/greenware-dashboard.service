[Unit]
Description=Greenware Raspberry Pi Dashboard
After=network.target

[Service]
User=pi
WorkingDirectory=/home/pi/greenware_pi_app/backend
ExecStart=/home/pi/greenware_pi_app/backend/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
